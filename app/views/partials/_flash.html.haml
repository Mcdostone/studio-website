- flash.each do |k, v|
  %div{:class => "alert alert-dismissible show animated fadeInRight alert-#{k}"}
    %button.close{type: 'button', data: {dismiss: 'alert'}, aria:{label: 'Close'}}
      %span{html: {aria: {:hidden => 'true'}}}
        &times;
    = v


    