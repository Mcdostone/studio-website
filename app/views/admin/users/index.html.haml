%h2 Liste des users
 
%table
  %tr
    %th ID
    %th Prénom
    %th Nom
    %th Pseudo
    %th.colum-timestamps Ajouté
    %th.colum-timestamps Mis à jour
 
  - @users.each do |user|
    %tr
      %td
        = user.id
      %td
        = user.first_name
      %td
        = user.last_name.upcase
      %td
        = user.nickname
      %td.colum-timestamps
        = format_date(user.created_at)
      %td.colum-timestamps
        = format_date(user.updated_at)
      %td
        = link_to 'Show', admin_user_path(user)
      %td
        = link_to 'Éditer', edit_admin_user_path(user)
      %td 
        = link_to 'Supprimer', admin_user_path(user), method: :delete, data: { confirm: 'Voulez vous supprimer ce user ?' }